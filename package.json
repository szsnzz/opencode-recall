{
  "name": "opencode-memory",
  "version": "0.1.0",
  "description": "Long-term memory plugin for opencode: explicit write + on-demand FTS5 search",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test \"test/*.test.ts\""
  },
  "keywords": [
    "opencode",
    "plugin",
    "memory",
    "fts5"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.17.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.17.3",
    "@types/bun": "latest",
    "typescript": "^5.8.0"
  }
}
